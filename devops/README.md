## Configuring `ntp` in ubuntu

```
sudo apt-get install ntp
```

Update `ntp` configuration

```
sudo vi /etc/ntp.conf
```

After updating configuration, restart `ntp` server

```
sudo service ntp restart
```

https://askubuntu.com/a/178977
